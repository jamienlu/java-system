server:
  port: 3401
#activemq配置
spring:
  activemq:
    broker-url: tcp://127.0.0.1:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: false   #默认是Queue队列模式 是false 主题是Topic 为true
#自己定义自己的队列
queue:
  activemq: boot-activemq-queue
  fixedDelay: 5000
  topicName: boot-activemq-topic
